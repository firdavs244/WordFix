================================================================================
                    WORDFIX BACKEND — TO'LIQ TEXNIK HUJJAT
================================================================================
Loyiha:      WordFix Backend
Versiya:     1.0.0
Tavsif:      AI-powered ingliz tili lug'at o'rganish platformasi
Python:      >=3.11, <4.0
Django:      5.1+
Framework:   Django REST Framework 3.15+
Arxitektura: Clean Architecture (Domain → Application → Infrastructure →
             Presentation)
Sana:        2026-yil, 13-fevral
================================================================================


================================================================================
1. TEXNOLOGIYALAR VA KUTUBXONALAR
================================================================================

PRODUCTION KUTUBXONALAR:
  - Django 5.1                — Web framework
  - Django REST Framework 3.15 — REST API
  - django-cors-headers 4.6   — CORS sozlamalari
  - django-filter 24.3        — API filtrlash
  - django-celery-beat 2.7    — Celery beat scheduler
  - djangorestframework-simplejwt 5.3 — JWT autentifikatsiya
  - django-allauth 65.0+      — Social autentifikatsiya (Google OAuth)
  - dj-rest-auth 7.0+         — REST autentifikatsiya (JWT rejimi)
  - psycopg2-binary 2.9       — PostgreSQL driver
  - redis 5.2                 — Redis client
  - django-redis 5.4          — Django cache backend
  - celery[redis] 5.4         — Task queue
  - gunicorn 23.0             — Production WSGI server
  - python-decouple 3.8       — Environment o'zgaruvchilari
  - whitenoise 6.8            — Statik fayllar
  - structlog 24.4            — Strukturalangan loglar
  - drf-spectacular 0.27+     — OpenAPI/Swagger hujjatlash
  - groq 0.12+                — Groq AI API (Llama 3.3)
  - openai 1.50+              — OpenAI API (GPT-4o-mini)
  - gTTS 2.5+                 — Google Text-to-Speech
  - Pillow 10.0+              — Rasm ishlash

DEV KUTUBXONALAR:
  - pytest 8.3                — Test framework
  - pytest-django 4.9         — Django pytest plugin
  - pytest-cov 6.0            — Test coverage
  - factory-boy 3.3           — Test ma'lumotlar factory
  - faker 33.0                — Fake ma'lumotlar generatori
  - black 24.10               — Kod formatlash (88 belgi)
  - isort 5.13                — Import tartiblash (black profil)
  - flake8 7.1                — Linter
  - mypy 1.13                 — Type tekshirish (django-stubs bilan)
  - django-stubs 5.1          — Django type stubs
  - djangorestframework-stubs 3.15 — DRF type stubs
  - django-debug-toolbar 4.4  — Debug toolbar
  - ipython 8.30              — Interaktiv Python shell
  - freezegun 1.4             — Vaqt muzlatish (testlarda)


================================================================================
2. SOZLAMALAR (Settings)
================================================================================

INSTALLED_APPS:
  Django:       admin, auth, contenttypes, sessions, messages, staticfiles
  Third-party:  rest_framework, rest_framework_simplejwt.token_blacklist,
                corsheaders, django_celery_beat, django_filters,
                drf_spectacular, allauth, allauth.account,
                allauth.socialaccount, allauth.socialaccount.providers.google,
                dj_rest_auth, dj_rest_auth.registration
  Local:        apps.common, apps.users, apps.words

AUTH_USER_MODEL: users.CustomUser

MIDDLEWARE (tartib bo'yicha):
  1.  SecurityMiddleware
  2.  WhiteNoiseMiddleware (statik fayllar)
  3.  CorsMiddleware
  4.  SessionMiddleware
  5.  CommonMiddleware
  6.  CsrfViewMiddleware
  7.  AuthenticationMiddleware
  8.  MessageMiddleware
  9.  XFrameOptionsMiddleware
  10. AllauthAccountMiddleware (social auth)
  11. RequestIDMiddleware (custom — X-Request-ID header)

REST_FRAMEWORK SOZLAMALARI:
  - Autentifikatsiya:     JWT (Bearer token)
  - Ruxsat:               IsAuthenticated (default)
  - Pagination:           StandardResultsPagination (20/sahifa, max 100)
  - Renderer:             CustomJSONRenderer (standart javob formati)
  - Filtrlash:            DjangoFilterBackend, SearchFilter, OrderingFilter
  - Exception Handler:    custom_exception_handler
  - Throttle:             anon=100/soat, user=1000/soat
  - Schema:               drf-spectacular AutoSchema

JWT SOZLAMALARI:
  - Access token:         60 daqiqa
  - Refresh token:        7 kun
  - Rotate refresh:       Ha
  - Blacklist after:      Ha
  - Auth header:          "Bearer"

MA'LUMOTLAR BAZASI:
  - Asosiy:  PostgreSQL 16 (db: wordfix, user: wordfix_user)
  - Fallback: SQLite (DB environment o'zgaruvchilari yo'q bo'lsa)
  - Test:    SQLite :memory:
  - Port:    5432 (ichki), 5433 (tashqi)

CACHE:
  - Asosiy:   Redis (django_redis), prefix: "wordfix"
  - Fallback: LocMemCache (Redis mavjud bo'lmasa)
  - Test:     LocMemCache

AI PROVIDER SOZLAMALARI:
  - Default provider:     groq
  - Groq model:           llama-3.3-70b-versatile
  - OpenAI model:         gpt-4o-mini
  - Max retries:          3
  - Timeout:              30 soniya
  - Enrichment enabled:   True

CORS:
  - Development:          Barcha originlarga ruxsat
  - Production:           localhost:5173, localhost:3000, 127.0.0.1:5173
  - Credentials:          Ha

ALLAUTH SOZLAMALARI:
  - Login metodi:         email
  - Email majburiy:       Ha
  - Username majburiy:    Ha
  - Google OAuth:         qo'llab-quvvatlanadi
  - Social account auto-connect: Ha

LOGGING:
  - Handlerlar:           console (StreamHandler) + file (logs/wordfix.log)
  - Format:               JSON (structlog)
  - 'apps' logger:        DEBUG darajasi
  - Root logger:          INFO darajasi
  - Max fayl hajmi:       10MB, 5 ta backup

SETTINGS VARIANTLARI:
  development.py — DEBUG=True, ALLOWED_HOSTS=["*"], console email,
                   CORS_ALLOW_ALL_ORIGINS=True, log level: DEBUG
  production.py  — DEBUG=False, HTTPS majburiy, HSTS=1 yil,
                   X_FRAME_OPTIONS="DENY", log level: WARNING/INFO
  test.py        — SQLite :memory:, MD5 password hasher,
                   CELERY_TASK_ALWAYS_EAGER=True, throttle o'chirilgan,
                   enrichment o'chirilgan, InMemoryStorage


================================================================================
3. URL PATTERNS (API Endpointlar)
================================================================================

ASOSIY URL PREFIXLAR:
  admin/                         — Django admin panel
  api/schema/                    — OpenAPI schema (JSON/YAML)
  api/docs/                      — Swagger UI (drf-spectacular)
  api/v1/                        — Users API (auth, profil, progress)
  api/v1/auth/onboarding/        — Onboarding API (daraja aniqlash)
  api/v1/words/                  — Words API (so'z CRUD, boyitish)
  api/v1/words/confusing-pairs/  — Confusing Pairs API (chalkash so'zlar)
  api/v1/review/                 — Review API (ko'rib chiqish)
  api/v1/tests/                  — Tests API (test generatsiya)
  api/v1/games/                  — Games API (5 ta o'yin turi)
  api/v1/chat/                   — Chat API (AI suhbat)
  api/v1/analytics/              — Analytics API (analitika)
  api/v1/challenges/             — Challenges API (kunlik topshiriqlar)
  api/v1/notifications/          — Notifications API (bildirishnomalar)

--- USERS ENDPOINTLARI ---

  GET    api/v1/health/                    — Health check (AllowAny)
  POST   api/v1/auth/register/             — Ro'yxatdan o'tish
  POST   api/v1/auth/login/                — Kirish (email + parol)
  POST   api/v1/auth/logout/               — Chiqish (token blacklist)
  POST   api/v1/auth/token/refresh/        — Access token yangilash
  GET    api/v1/auth/profile/              — Profil olish
  PUT    api/v1/auth/profile/              — Profil yangilash
  POST   api/v1/auth/change-password/      — Parol almashtirish
  GET    api/v1/users/progress/            — Foydalanuvchi progressi
  GET    api/v1/users/xp-history/          — XP tarixi
  GET    api/v1/users/badges/              — Foydalanuvchi badgelari
  GET    api/v1/badges/                    — Barcha badgelar ro'yxati

--- ONBOARDING ENDPOINTLARI ---

  GET    api/v1/auth/onboarding/questions/ — Onboarding savollari (AllowAny)
  POST   api/v1/auth/onboarding/submit/    — Javoblarni yuborish
  POST   api/v1/auth/onboarding/skip/      — Onboardingni o'tkazib yuborish
  GET    api/v1/auth/onboarding/status/    — Onboarding holati

--- NOTIFICATION ENDPOINTLARI ---

  GET    api/v1/notifications/             — Bildirishnomalar ro'yxati
  POST   api/v1/notifications/read/        — O'qilgan deb belgilash
  GET    api/v1/notifications/unread-count/ — O'qilmaganlar soni

--- WORDS ENDPOINTLARI ---

  GET    api/v1/words/                     — So'zlar ro'yxati (paginated)
  POST   api/v1/words/                     — So'z qo'shish + auto-enrich
  POST   api/v1/words/bulk/                — Ko'p so'z birdan qo'shish
  GET    api/v1/words/stats/               — So'z statistikasi
  GET    api/v1/words/review/              — Ko'rib chiqish uchun so'zlar
  POST   api/v1/words/enrich-all/          — Barcha so'zlarni AI bilan boyitish
  POST   api/v1/words/import/analyze/      — Matnni AI bilan tahlil qilish
  POST   api/v1/words/import/add/          — So'zlarni import qilish
  GET    api/v1/words/{id}/                — So'z to'liq detali
  PUT    api/v1/words/{id}/                — So'z yangilash
  DELETE api/v1/words/{id}/                — So'z o'chirish
  POST   api/v1/words/{id}/enrich/         — Bitta so'zni AI bilan boyitish
  GET    api/v1/words/{id}/enrichment-status/ — Boyitish holati
  GET    api/v1/words/categories/          — Kategoriyalar ro'yxati
  POST   api/v1/words/categories/          — Kategoriya yaratish
  DELETE api/v1/words/categories/{id}/     — Kategoriya o'chirish

--- CONFUSING PAIRS ENDPOINTLARI ---

  GET    api/v1/words/confusing-pairs/            — Chalkash juftliklar
  GET    api/v1/words/confusing-pairs/count/       — Hal qilinmaganlar soni
  GET    api/v1/words/confusing-pairs/{id}/        — Juftlik detali
  POST   api/v1/words/confusing-pairs/{id}/drill/  — AI mashq generatsiya
  POST   api/v1/words/confusing-pairs/{id}/resolve/ — Juftlikni hal qilish

--- REVIEW ENDPOINTLARI ---

  GET    api/v1/review/words/              — Ko'rib chiqish uchun so'zlar
  POST   api/v1/review/sessions/           — Sessiya boshlash
  GET    api/v1/review/sessions/{id}/      — Sessiya detali
  POST   api/v1/review/sessions/{id}/answer/ — Javob yuborish (SM-2)
  POST   api/v1/review/sessions/{id}/complete/ — Sessiya tugatish
  GET    api/v1/review/summary/            — Umumiy xulosa
  GET    api/v1/review/history/            — Ko'rib chiqish tarixi
  GET    api/v1/review/streak/             — Streak ma'lumotlari
  GET    api/v1/review/daily-progress/     — Kunlik progress
  GET    api/v1/review/words/{id}/predict/ — SM-2 bashorat intervallari

--- TEST ENDPOINTLARI ---

  POST   api/v1/tests/generate/            — AI test generatsiya qilish
  GET    api/v1/tests/history/             — Test tarixi
  GET    api/v1/tests/{id}/                — Test detali + savollar
  POST   api/v1/tests/{id}/answer/         — Javob yuborish
  POST   api/v1/tests/{id}/complete/       — Test tugatish

--- GAME ENDPOINTLARI ---

  POST   api/v1/games/speed-round/start/   — Speed Round boshlash
  POST   api/v1/games/speed-round/submit/  — Speed Round natijalarini yuborish
  POST   api/v1/games/word-match/start/    — Word Match boshlash
  POST   api/v1/games/word-match/submit/   — Word Match yuborish
  POST   api/v1/games/word-context/start/  — Word Context boshlash
  POST   api/v1/games/word-context/submit/ — Word Context yuborish
  POST   api/v1/games/story-builder/start/ — Story Builder boshlash
  POST   api/v1/games/story-builder/submit/ — Story Builder yuborish
  POST   api/v1/games/listening/start/     — Listening Challenge boshlash
  POST   api/v1/games/listening/submit/    — Listening Challenge yuborish
  GET    api/v1/games/history/             — O'yin tarixi (paginated)
  GET    api/v1/games/stats/               — O'yin statistikasi

--- CHAT ENDPOINTLARI ---

  POST   api/v1/chat/start/                — AI chat boshlash (mavzu)
  GET    api/v1/chat/history/              — Chat tarixi (paginated)
  GET    api/v1/chat/sessions/{id}/        — Chat sessiya detali
  POST   api/v1/chat/sessions/{id}/message/ — Xabar yuborish → AI javob
  POST   api/v1/chat/sessions/{id}/end/    — Chat tugatish

--- ANALYTICS ENDPOINTLARI ---

  GET    api/v1/analytics/overview/        — Umumiy ko'rinish
  GET    api/v1/analytics/weekly/          — Haftalik statistika (7 kun)
  GET    api/v1/analytics/monthly/         — Oylik statistika (30 kun)
  GET    api/v1/analytics/difficult-words/ — Eng qiyin so'zlar (top 10)
  GET    api/v1/analytics/word-progress/   — So'z progressi taqsimoti
  GET    api/v1/analytics/calendar/        — O'qish kalendari (heatmap)

--- CHALLENGES ENDPOINTLARI ---

  GET    api/v1/challenges/today/          — Bugungi kunlik topshiriqlar
  POST   api/v1/challenges/claim/          — Kunlik bonus olish


================================================================================
4. MA'LUMOTLAR BAZASI MODELLARI
================================================================================

UMUMIY BAZAVIY MODEL (AbstractBaseModel):
  - id              UUID    (PK, auto uuid4)
  - created_at      DateTime (auto_now_add)
  - updated_at      DateTime (auto_now)
  - is_active       Boolean  (default: True)
  Metodlar: soft_delete(), restore()

=== USERS APP MODELLARI ===

CustomUser (AbstractBaseUser, PermissionsMixin):
  Jadval: users
  - id                       UUID     PK
  - email                    Email    unique, indexed
  - username                 Char(30) unique, regex: [a-zA-Z0-9_]
  - full_name                Char(100)
  - avatar                   Image    upload_to="avatars/"
  - native_language          Char(10) default: "uz"
  - learning_language        Char(10) default: "en"
  - proficiency_level        Char(2)  choices: A1/A2/B1/B2/C1/C2
  - daily_goal               Integer  default: 10
  - timezone                 Char(50) default: "Asia/Tashkent"
  - is_premium               Boolean  default: False
  - premium_until            DateTime nullable
  - has_completed_onboarding Boolean  default: False
  - is_active                Boolean  default: True
  - is_staff                 Boolean  default: False
  - date_joined              DateTime auto_now_add
  - last_login               DateTime nullable
  USERNAME_FIELD: email
  REQUIRED_FIELDS: ["username"]
  Manager: CustomUserManager (create_user, create_superuser)
  Property: is_premium_active

UserProgress:
  Jadval: user_progress
  - id                       UUID PK
  - user                     OneToOne(CustomUser) → related: progress
  - total_xp                 Integer (0)
  - level                    Integer (1)
  - words_learned_total      Integer (0)
  - words_mastered_total     Integer (0)
  - tests_completed          Integer (0)
  - games_played             Integer (0)
  - reviews_completed        Integer (0)
  - perfect_scores           Integer (0)
  - total_study_time_seconds Integer (0)
  - created_at / updated_at  DateTime

XPTransaction:
  Jadval: xp_transactions
  - id          UUID PK
  - user        FK(CustomUser) → related: xp_transactions
  - amount      Integer
  - reason      Char(20) — 16 ta turli sabab
  - description Char(200)
  - created_at  DateTime

Badge:
  Jadval: badges
  - id          UUID PK
  - code        Char(50) unique
  - name        Char(100)
  - description Char(255)
  - icon        Char(50) default: "award"
  - category    Char(10) choices: words/streak/review/test/game/mastery/level
  - xp_reward   Integer (0)
  - rarity      Char(10) choices: common/rare/epic/legendary
  - created_at  DateTime

UserBadge:
  Jadval: user_badges
  - id        UUID PK
  - user      FK(CustomUser)
  - badge     FK(Badge)
  - earned_at DateTime (auto_now_add)
  unique_together: [user, badge]

Notification:
  Jadval: notifications
  - id         UUID PK
  - user       FK(CustomUser)
  - type       Char(25) — 7 ta bildirishnoma turi
  - title      Char(200)
  - message    Text
  - is_read    Boolean (False)
  - data       JSON (dict)
  - created_at DateTime

OnboardingQuestion:
  Jadval: onboarding_questions
  - id       UUID PK
  - question Text
  - options  JSON (list)
  - order    Integer

OnboardingResult:
  Jadval: onboarding_results
  - id               UUID PK
  - user             OneToOne(CustomUser)
  - answers          JSON (list)
  - determined_level Char(2) — A1/A2/B1/B2/C1/C2
  - completed_at     DateTime

=== WORDS APP MODELLARI ===

WordCategory (AbstractBaseModel):
  Jadval: word_categories
  - user        FK → related: word_categories
  - name        Char(50)
  - color       Char(7) default: "#6C5CE7"
  - icon        Char(50) default: "book"
  - words_count Integer (0)
  unique_together: [user, name]

Word (AbstractBaseModel):
  Jadval: words
  - user              FK → related: words, indexed
  - original_word     Char(100) indexed
  - translation       Char(200)
  - pronunciation     Char(200)
  - part_of_speech    Char(15) — 10 ta tanlov
  - definition        Text
  - example_sentence  Text
  - example_translation Text
  - synonyms          JSON (list)
  - antonyms          JSON (list)
  - collocations      JSON (list)
  - word_family       JSON (list)
  - image_url         URL
  - audio_url         URL
  - notes             Text
  - mnemonic          Text
  - usage_notes       Text
  - category          FK(WordCategory) nullable
  - tags              JSON (list)
  - difficulty_level  Char(6) — easy/medium/hard
  - is_enriched       Boolean (False)
  - enrichment_status Char(10) — pending/enriching/enriched/failed
  - enrichment_error  Text
  - enriched_at       DateTime nullable
  - confidence_score  Float (0, 0–100)
  - next_review_at    DateTime nullable
  - review_count      Integer (0)
  - correct_count     Integer (0)
  - incorrect_count   Integer (0)
  - last_reviewed_at  DateTime nullable
  - is_mastered       Boolean (False)
  - easiness_factor   Float (2.5) — SM-2
  - repetition_number Integer (0) — SM-2
  - interval_days     Integer (0) — SM-2
  unique_together: [user, original_word]
  Indexes: [user,is_mastered], [user,confidence_score],
           [user,next_review_at], [user,category]

ReviewSession (AbstractBaseModel):
  Jadval: review_sessions
  - user, started_at, completed_at, total_words, correct_count,
    incorrect_count, average_quality, duration_seconds,
    session_type (review/quick/focus), is_completed

ReviewLog (AbstractBaseModel):
  Jadval: review_logs
  - session, user, word, quality (0–5), response_time_ms, is_correct,
    reviewed_at, previous_confidence, new_confidence,
    previous_interval, new_interval

DailyStreak (AbstractBaseModel):
  Jadval: daily_streaks
  - user (OneToOne), current_streak, longest_streak,
    last_activity_date, streak_frozen_until, total_review_days

DailyActivity (AbstractBaseModel):
  Jadval: daily_activities
  - user, date, words_reviewed, words_added, words_mastered,
    correct_answers, incorrect_answers, total_time_seconds,
    goal_completed, xp_earned
  unique_together: [user, date]

TestSession (AbstractBaseModel):
  Jadval: test_sessions
  - user, test_type, difficulty, total_questions, correct_answers,
    incorrect_answers, score_percentage, started_at, completed_at,
    duration_seconds, is_completed, words_tested (M2M)

TestQuestion (AbstractBaseModel):
  Jadval: test_questions
  - session, word, question_type (6 tur), question_text, correct_answer,
    options (JSON), user_answer, is_correct, response_time_ms,
    explanation, order

GameSession (AbstractBaseModel):
  Jadval: game_sessions
  - user, game_type (5 tur), score, max_score, correct_answers,
    incorrect_answers, duration_seconds, started_at, completed_at,
    is_completed, level, xp_earned

StoryRound (AbstractBaseModel):
  Jadval: story_rounds
  - session (FK GameSession), paragraph, user_continuation,
    ai_feedback, grammar_corrections (JSON), score, order

ListeningRound (AbstractBaseModel):
  Jadval: listening_rounds
  - session (FK GameSession), word (FK Word), audio_url,
    user_answer, is_correct, hint_used, order

ChatSession (AbstractBaseModel):
  Jadval: chat_sessions
  - user, topic, started_at, ended_at, message_count,
    words_practiced (M2M), target_words (JSON), is_active

ChatMessage (AbstractBaseModel):
  Jadval: chat_messages
  - session (FK ChatSession), role (user/assistant), content,
    corrections (JSON), words_used (JSON), order

DailyChallenge (AbstractBaseModel):
  Jadval: daily_challenges
  - user, date, challenges (JSON), all_completed, bonus_claimed
  unique_together: [user, date]

ConfusingPair (AbstractBaseModel):
  Jadval: confusing_pairs
  - user, word_1 (FK Word), word_2 (FK Word), confusion_count,
    last_confused_at, is_resolved, drill_data (JSON)
  unique_together: [user, word_1, word_2]


================================================================================
5. DOMAIN QATLAMI (Domain Layer)
================================================================================

WORDS DOMAIN:

  Entities (apps/words/domain/entities.py):
    - WordCategoryEntity     (validate() metodi)
    - WordEntity             (accuracy_rate property, validate())
    - ReviewSessionEntity
    - ReviewLogEntity
    - DailyStreakEntity
    - DailyActivityEntity
    - TestSessionEntity
    - TestQuestionEntity
    - GameSessionEntity
    - ChatSessionEntity
    - ChatMessageEntity

  Value Objects (apps/words/domain/value_objects.py):
    - WordText (frozen)  — bo'sh emas, max 100 belgi, lowercase
    - Definition (frozen) — bo'sh emas

  Domain Services (apps/words/domain/services.py):
    - SpacedRepetitionService — SM-2 algoritmi implementatsiyasi
      • calculate_next_review(word, quality, now) → word
      • calculate_confidence(word, now) → float (0-100)
      • get_predicted_intervals(word) → dict
    - WordEnrichmentDomainService
      • enrich_word(word, enrichment_data, now) → word

  Repository Interfeyslari (apps/words/domain/repositories.py):
    10 ta abstract repository interfeysi (14+ metod har birida)

USERS DOMAIN:

  Entities — UserEntity (validate_email, validate_username)
  Value Objects — Email, Password (frozen dataclass)
  Repository — AbstractUserRepository (7 metod)


================================================================================
6. APPLICATION QATLAMI (Use Cases)
================================================================================

  WORD CRUD:          8 ta use case (Add, Get, Update, Delete, Bulk, Stats...)
  ENRICHMENT:         2 ta (EnrichWord, BatchEnrich)
  REVIEW:             5 ta (GetWords, Start, Submit, Complete, Summary)
  STREAK:             1 ta (UpdateStreak)
  TESTING:            5 ta (Generate, Submit, Complete, History, Detail)
  GAMES:              12 ta (Start/Submit × 5 turi + History + Stats)
  SMART IMPORT:       2 ta (AnalyzeText, ImportWords)
  CHAT:               5 ta (Start, SendMessage, End, History, Detail)
  ANALYTICS:          6 ta (Overview, Weekly, Monthly, Difficult, Progress,
                      Calendar)
  USERS:              5 ta (Register, Login, GetProfile, UpdateProfile,
                      ChangePassword)

  JAMI: 50+ ta use case


================================================================================
7. USERS DOMAIN SERVICES
================================================================================

XPService:
  Daraja chegaralari: L1=0, L2=100, L3=250, L4=500, L5=1000, L6=1750,
                      L7=2750, L8=4000, L9=5500, L10=7500, L11+=+2500
  XP mukofotlari: word_added=5, review_correct=10, review_complete=20,
                  test_complete=25, game_complete=15, word_mastered=25,
                  streak_7=50, streak_30=200, daily_goal=20

BadgeService:
  20 ta badge (common → legendary): birinchi so'z, 10/50/100 so'z,
  3/7/30/100 streak, birinchi review/test/game, perfect review/test,
  speed demon, level 5/10, mastered 10/50

NotificationService:
  7 ta bildirishnoma turi: review_reminder, streak_warning, badge_earned,
  level_up, daily_goal_complete, word_mastered, weekly_report


================================================================================
8. INFRASTRUCTURE QATLAMI
================================================================================

REPOSITORY IMPLEMENTATSIYALARI:
  Words:  5 ta repository fayl (word, review, test, game, chat)
  Users:  5 ta repository fayl (user, progress, xp, badge, notification)

MODELS: 7 ta model fayl (word, review, test, game, chat, challenge, confusing)
TASKS:  2 ta task fayl (words tasks, users tasks)


================================================================================
9. PRESENTATION QATLAMI
================================================================================

VIEWS:        50+ ta view class (10+ ta fayl)
SERIALIZERS:  30+ ta serializer (6+ ta fayl)
DEPENDENCIES: 9+ ta dependency factory fayl
URLS:         8 ta URL configuration fayl


================================================================================
10. CORE — AI VA TTS XIZMATLARI
================================================================================

AI PROVIDERLAR:
  GroqProvider    — llama-3.3-70b-versatile (default, tezkor)
  OpenAIProvider  — gpt-4o-mini (alternativ)
  AIProviderFactory — Singleton, provider nomi bo'yicha

TTS PROVIDER:
  GoogleTTSProvider — gTTS (bepul, MP3 format)

AI PROMPTLAR (7 ta):
  WORD_ENRICHMENT_PROMPT, MULTIPLE_CHOICE_PROMPT, FILL_BLANK_PROMPT,
  CONTEXT_GUESS_PROMPT, WORD_CONTEXT_GAME_PROMPT, SMART_IMPORT_PROMPT,
  CHAT_SYSTEM_PROMPT

  Qo'llab-quvvatlanadigan tillar: uz, ru, tr, ko, ar, de, fr, es,
                                  ja, zh, hi, pt


================================================================================
11. CELERY VAZIFALAR VA JADVAL
================================================================================

CELERY SOZLAMALARI:
  Broker:      Redis (redis://redis:6379/1)
  Result:      Redis (redis://redis:6379/2)
  Scheduler:   DatabaseScheduler
  Time limit:  300 soniya

BEAT JADVAL (5 ta task):
  update-streaks-daily     — Har kuni
  cleanup-stale-sessions   — Har soatda
  review-reminder-morning  — Har 12 soatda
  streak-warning-evening   — Har kuni
  weekly-report            — Har hafta


================================================================================
12. COMMON MODULE
================================================================================

EXCEPTIONS:  8 ta custom exception (400–503)
HANDLER:     custom_exception_handler (standart format)
PAGINATION:  StandardResultsPagination (20/sahifa, max 100)
PERMISSIONS: IsActiveUser, IsOwner
RENDERER:    CustomJSONRenderer ({success, data, message, errors, meta})
MIDDLEWARE:  RequestIDMiddleware (X-Request-ID)
HELPERS:     build_success_response, build_error_response, get_client_ip


================================================================================
13. TEST INFRASTRUKTURASI
================================================================================

CONFTEST.PY FIKSTURALARI:
  api_client, user, another_user, authenticated_client,
  another_authenticated_client, word_category, sample_word, sample_words

PYTEST: -v --tb=short --strict-markers
SETTINGS: config.settings.test (SQLite :memory:, eager Celery)
TEST PAPKALAR: apps/, core/, tests/


================================================================================
14. DOCKER SOZLAMALARI
================================================================================

DOCKERFILE:  python:3.12-slim, poetry install, entrypoint.sh
ENTRYPOINT:  DB kutish → migrate → collectstatic → CMD
COMPOSE:     6 ta service (db, redis, web, celery_worker, celery_beat, frontend)
VOLUMES:     postgres_data, redis_data, media_data
NETWORK:     wordfix-network (bridge)


================================================================================
15. FAYL STRUKTURASI — BACKEND
================================================================================

wordfix-backend/
├── conftest.py
├── manage.py
├── pyproject.toml
├── apps/
│   ├── common/              — Umumiy yordamchi modullar
│   │   ├── exception_handler.py, exceptions.py, models.py
│   │   ├── pagination.py, permissions.py, renderers.py
│   │   ├── middleware/request_id.py
│   │   ├── utils/helpers.py
│   │   └── tests/           — 8 ta test fayl
│   ├── users/               — Foydalanuvchilar ilovasi
│   │   ├── application/use_cases.py
│   │   ├── domain/entities.py, repositories.py, value_objects.py
│   │   ├── domain/services/xp_service.py, badge_service.py,
│   │   │                    notification_service.py
│   │   ├── infrastructure/models.py, tasks.py
│   │   ├── infrastructure/repositories/ (5 ta fayl)
│   │   ├── presentation/views.py, serializers.py, urls.py, ...
│   │   └── tests/
│   └── words/               — So'zlar ilovasi
│       ├── application/use_cases/ (10+ ta fayl)
│       ├── domain/entities.py, repositories.py, services.py, ...
│       ├── infrastructure/models/ (7 ta fayl)
│       ├── infrastructure/repositories/ (5 ta fayl)
│       ├── infrastructure/tasks.py
│       ├── presentation/views/ (10+ ta fayl)
│       ├── presentation/serializers/ (6+ ta fayl)
│       ├── presentation/dependencies/ (9+ ta fayl)
│       ├── presentation/urls.py + 7 ta URL fayl
│       └── tests/
├── config/
│   ├── urls.py, celery.py, asgi.py, wsgi.py
│   └── settings/base.py, development.py, production.py, test.py
├── core/
│   ├── interfaces/ai_provider.py, tts_provider.py
│   ├── services/ai/ (4 ta fayl), tts/ (1 ta fayl)
│   └── tests/ (7 ta fayl)
├── docker/Dockerfile, entrypoint.sh
├── logs/, media/, staticfiles/
└── tests/test_docker_health.py, test_settings.py

================================================================================
              BACKEND HUJJAT TUGADI — 15 BO'LIM
================================================================================

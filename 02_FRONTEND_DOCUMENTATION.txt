================================================================================
                    WORDFIX FRONTEND — TO'LIQ TEXNIK HUJJAT
================================================================================
Loyiha:      WordFix Frontend
Versiya:     1.0.0
Framework:   React 19 + TypeScript 5.6
Bundler:     Vite 6.0
State:       Zustand (client) + TanStack React Query (server)
Styling:     Tailwind CSS 3.4 + Radix UI
Animatsiya:  Framer Motion 12
Sana:        2026-yil, 13-fevral
================================================================================


================================================================================
1. TEXNOLOGIYALAR VA KUTUBXONALAR
================================================================================

ASOSIY KUTUBXONALAR:
  - React 19.0.0               — UI framework
  - TypeScript 5.6             — Tip xavfsizligi
  - Vite 6.0                   — Build tool va dev server
  - React Router DOM 7.1       — Client-side routing
  - TanStack React Query 5.62  — Server state management
  - Zustand 5.0                — Client state management
  - Axios 1.7                  — HTTP client
  - Tailwind CSS 3.4           — Utility-first CSS
  - Framer Motion 12.0         — Animatsiyalar
  - Lucide React 0.469         — Icon kutubxonasi
  - Sonner 1.7                 — Toast bildirishnomalar

UI KOMPONENTLAR (Radix UI — headless, accessible):
  - @radix-ui/react-avatar     — Avatar
  - @radix-ui/react-dialog     — Modal dialog
  - @radix-ui/react-dropdown-menu — Dropdown menyu
  - @radix-ui/react-label      — Form label
  - @radix-ui/react-popover    — Popover
  - @radix-ui/react-select     — Select input
  - @radix-ui/react-separator  — Separator
  - @radix-ui/react-slot       — Slot
  - @radix-ui/react-switch     — Toggle switch
  - @radix-ui/react-tabs       — Tablar
  - @radix-ui/react-tooltip    — Tooltip

YORDAMCHI KUTUBXONALAR:
  - date-fns 4.1               — Sana formatlash utilitalari
  - cmdk 1.0                   — Command palette (Ctrl+K menyu)
  - canvas-confetti 1.9        — Confetti effekt (muvaffaqiyat)
  - class-variance-authority    — Komponent variantlari
  - clsx + tailwind-merge       — CSS class birlashtirish

FONTLAR:
  - Inter (400, 500, 600, 700)       — Asosiy matn
  - Space Grotesk (500, 600, 700)    — Sarlavhalar

DEV KUTUBXONALAR:
  - @tanstack/react-query-devtools — DevTools (development)
  - eslint + typescript-eslint     — Linting
  - eslint-plugin-react-hooks      — React hooks qoidalari
  - eslint-plugin-react-refresh    — Hot refresh qoidalari
  - postcss + autoprefixer         — CSS post-processing


================================================================================
2. LOYIHA KONFIGURATSIYASI
================================================================================

VITE CONFIG (vite.config.ts):
  - Path alias: @/ → ./src/
  - Dev proxy: /api → http://web:8000
  - React plugin (@vitejs/plugin-react)
  - Port: 5173

TSCONFIG:
  - Target: ES2020
  - Strict mode: true
  - Path alias: @/* → src/*
  - JSX: react-jsx

TAILWIND CONFIG (tailwind.config.ts):
  - Dark mode: class-based
  - Custom CSS o'zgaruvchilar (HSL rang tizimi)
  - tailwindcss-animate plugin
  - Custom breakpoints

DOCKER:
  - Base: node:20-alpine
  - Port: 5173
  - Command: npm run dev -- --host 0.0.0.0
  - Environment: VITE_API_URL=http://localhost:8000/api/v1

PACKAGE.JSON SCRIPTS:
  - dev:     vite --host 0.0.0.0 (hot reload dev server)
  - build:   tsc -b && vite build (production build)
  - lint:    eslint . (kod tekshirish)
  - preview: vite preview (build preview)


================================================================================
3. DASTUR BOSHLANG'ICH NUQTASI
================================================================================

BOOTSTRAP ZANJIRI (main.tsx):
  StrictMode
    → BrowserRouter
      → ThemeProvider (storageKey="wordfix-theme")
        → QueryProvider (staleTime=5min, retry=1)
          → App
          → Toaster (position="top-right", richColors)

App.tsx:
  Faqat <AppRoutes /> ni render qiladi


================================================================================
4. ROUTING TIZIMI (src/routes/index.tsx)
================================================================================

ROUTE GUARD'LAR:
  ProtectedRoute — useAuthStore().isAuthenticated tekshiradi,
                   /login ga redirect qiladi
  PublicRoute    — Autentifikatsiya qilingan foydalanuvchilarni / ga
                   redirect qiladi

LAZY LOADING:
  Barcha sahifalar React.lazy() orqali yuklanadi (code splitting)
  Suspense fallback: <LoadingScreen />

BARCHA MARSHRYTLAR:

  OMMAVIY (Public — PublicRoute ichida):
    /welcome              — LandingPage (landing sahifa)
    /login                — LoginPage (AuthLayout)
    /register             — RegisterPage (AuthLayout)

  HIMOYALANGAN (Protected — ProtectedRoute + RootLayout ichida):
    /                     — DashboardPage (bosh sahifa)
    /words                — WordsPage (so'zlar ro'yxati)
    /review               — ReviewPage (ko'rib chiqish)
    /review/session/:id   — ReviewSessionPage (sessiya)
    /review/complete/:id  — ReviewCompletePage (yakunlash)
    /review/history       — ReviewHistoryPage (tarix)
    /profile              — ProfilePage (profil)
    /tests                — TestPage (testlar)
    /tests/session/:id    — TestSessionPage (test sessiyasi)
    /tests/result/:id     — TestResultPage (natijalar)
    /games                — GamesPage (o'yinlar)
    /games/speed-round    — SpeedRoundPage
    /games/word-match     — WordMatchPage
    /games/word-context   — WordContextPage
    /games/story-builder  — StoryBuilderPage
    /games/listening      — ListeningChallengePage
    /games/result/:id     — GameResultPage
    /badges               — BadgesPage (badgelar)
    /notifications        — NotificationsPage (bildirishnomalar)
    /import               — ImportPage (smart import)
    /chat                 — ChatPage (AI chat)
    /chat/session/:id     — ChatSessionPage (chat sessiyasi)
    /chat/history         — ChatHistoryPage (chat tarixi)
    /analytics            — AnalyticsPage (analitika)
    /confusing-pairs      — ConfusingPairsPage (chalkash so'zlar)
    /onboarding           — OnboardingPage (daraja aniqlash)
    *                     — 404 sahifa (NotFound)

  JAMI: 30+ ta marshrut


================================================================================
5. API QATLAMI
================================================================================

API CLIENT (src/api/client.ts):
  Axios instance:
    Base URL: VITE_API_URL yoki http://localhost:8000/api/v1

  Request Interceptor:
    - localStorage dan access_token ni oladi
    - Authorization: Bearer <token> header qo'shadi

  Response Interceptor:
    - 401 → so'rovlar navbatga qo'yiladi → token refresh qilinadi →
            navbatdagi so'rovlar qayta yuboriladi (silent refresh)
    - 403 → "Access denied" toast
    - 500 → "Server error" toast
    - Network error → "Connection error" toast

API ENDPOINTLAR (src/api/endpoints.ts):

  AUTH:
    /auth/register/              — POST  Ro'yxatdan o'tish
    /auth/login/                 — POST  Kirish
    /auth/logout/                — POST  Chiqish
    /auth/token/refresh/         — POST  Token yangilash
    /auth/profile/               — GET/PUT Profil
    /auth/change-password/       — POST  Parol almashtirish

  ONBOARDING:
    /auth/onboarding/questions/  — GET   Onboarding savollari
    /auth/onboarding/submit/     — POST  Javoblarni yuborish
    /auth/onboarding/skip/       — POST  O'tkazib yuborish
    /auth/onboarding/status/     — GET   Holat

  WORDS:
    /words/                      — GET/POST So'zlar
    /words/{id}/                 — GET/PUT/DELETE So'z detali
    /words/bulk/                 — POST Ko'p qo'shish
    /words/stats/                — GET  Statistika
    /words/review/               — GET  Ko'rib chiqish so'zlari
    /words/categories/           — GET/POST Kategoriyalar
    /words/{id}/enrich/          — POST Boyitish
    /words/enrichment-status/    — GET  Boyitish holati

  CONFUSING PAIRS:
    /words/confusing-pairs/            — GET  Ro'yxat
    /words/confusing-pairs/count/      — GET  Soni
    /words/confusing-pairs/{id}/       — GET  Detal
    /words/confusing-pairs/{id}/drill/ — POST Mashq
    /words/confusing-pairs/{id}/resolve/ — POST Hal qilish

  REVIEW:
    /review/words/                     — GET  So'zlar
    /review/sessions/                  — POST Sessiya boshlash
    /review/sessions/{id}/answer/      — POST Javob
    /review/sessions/{id}/complete/    — POST Tugatish
    /review/summary/                   — GET  Xulosa
    /review/streak/                    — GET  Streak
    /review/daily-progress/            — GET  Kunlik progress

  TESTS:
    /tests/generate/                   — POST Test yaratish
    /tests/history/                    — GET  Tarix
    /tests/{id}/                       — GET  Detal
    /tests/{id}/answer/                — POST Javob
    /tests/{id}/complete/              — POST Tugatish

  GAMES:
    /games/speed-round/start/          — POST Speed Round
    /games/speed-round/submit/         — POST
    /games/word-match/start/           — POST Word Match
    /games/word-match/submit/          — POST
    /games/word-context/start/         — POST Word Context
    /games/word-context/submit/        — POST
    /games/story-builder/start/        — POST Story Builder
    /games/story-builder/submit/       — POST
    /games/listening/start/            — POST Listening Challenge
    /games/listening/submit/           — POST
    /games/history/                    — GET  Tarix
    /games/stats/                      — GET  Statistika

  PROGRESS:
    /users/progress/                   — GET Progress
    /users/xp-history/                 — GET XP tarixi
    /users/badges/                     — GET Badgelar
    /badges/                           — GET Barcha badgelar

  NOTIFICATIONS:
    /notifications/                    — GET  Ro'yxat
    /notifications/{id}/read/          — POST O'qilgan
    /notifications/unread-count/       — GET  Soni

  IMPORT:
    /words/import/analyze/             — POST Matn tahlili
    /words/import/add/                 — POST So'z qo'shish

  CHAT:
    /chat/start/                       — POST Boshlash
    /chat/history/                     — GET  Tarix
    /chat/sessions/{id}/               — GET  Detal
    /chat/sessions/{id}/message/       — POST Xabar
    /chat/sessions/{id}/end/           — POST Tugatish

  ANALYTICS:
    /analytics/overview/               — GET  Umumiy
    /analytics/weekly/                 — GET  Haftalik
    /analytics/monthly/                — GET  Oylik
    /analytics/difficult-words/        — GET  Qiyin so'zlar
    /analytics/word-progress/          — GET  Progress
    /analytics/calendar/               — GET  Kalendar

  CHALLENGES:
    /challenges/today/                 — GET  Bugungi
    /challenges/claim/                 — POST Bonus olish

FEATURE API MODULLARI (har bir feature o'z API fayli bilan):
  src/features/auth/api/authApi.ts            — 7 ta funksiya
  src/features/words/api/wordApi.ts           — 11 ta funksiya
  src/features/review/api/reviewApi.ts        — 13 ta funksiya
  src/features/tests/api/testApi.ts           — 5 ta funksiya
  src/features/games/api/gameApi.ts           — 8+ ta funksiya
  src/features/chat/api/chatApi.ts            — 5 ta funksiya
  src/features/import/api/importApi.ts        — 2 ta funksiya
  src/features/analytics/api/analyticsApi.ts  — 6 ta funksiya
  src/features/progress/api/progressApi.ts    — 8 ta funksiya
  src/features/challenges/api/challengeApi.ts — 2 ta funksiya
  src/features/confusing-pairs/api/confusingApi.ts — 5 ta funksiya
  src/features/onboarding/api/               — 4 ta funksiya


================================================================================
6. STATE MANAGEMENT
================================================================================

ZUSTAND STORELARI (client state — localStorage bilan sinxron):

useAppStore (src/stores/useAppStore.ts):
  State:
    sidebarCollapsed: boolean
  Actions:
    toggleSidebar()
    setSidebarCollapsed(collapsed)

useAuthStore (src/stores/useAuthStore.ts):
  State:
    user: User | null
    isAuthenticated: boolean
    isLoading: boolean
  Actions:
    setUser(user)
    setLoading(loading)
    login(credentials)   — authApi.login() → token saqlash → profil olish
    register(data)       — authApi.register() → token saqlash → profil olish
    logout()             — authApi.logout() → token o'chirish → state reset
    fetchProfile()       — profil API so'rov
    initialize()         — localStorage token tekshirish → profil yuklash

useReviewStore (src/stores/useReviewStore.ts):
  State:
    currentSession: ReviewSession | null
    words: Word[]
    currentIndex: number
    isFlipped: boolean
    answers: Map<string, ReviewQuality>
  Actions:
    setSession, setWords, flipCard, setFlipped, recordAnswer,
    nextWord, previousWord, reset
  Computed:
    currentWord, progress, isLastWord, totalAnswered

REACT QUERY (server state):
  QueryClient sozlamalari:
    staleTime:            5 daqiqa (300,000ms)
    retry:                1 marta
    refetchOnWindowFocus: false
    mutations retry:      0


================================================================================
7. REACT QUERY HOOKLARI
================================================================================

Barcha hooklar query key factory pattern dan foydalanadi.

WORDS HOOKS (src/features/words/hooks/useWords.ts):
  useWords(filters)       — Query ['words','list',filters] keepPreviousData
  useWord(id)             — Query ['words','detail',id] enabled=!!id
  useWordStats()          — Query ['words','stats']
  useReviewWords()        — Query ['words','review']
  useCategories()         — Query ['words','categories']
  useCreateWord()         — Mutation → invalidates words lists
  useUpdateWord()         — Mutation → invalidates words lists + detail
  useDeleteWord()         — Mutation → invalidates words lists
  useBulkCreateWords()    — Mutation → invalidates words lists
  useCreateCategory()     — Mutation → invalidates categories
  useDeleteCategory()     — Mutation → invalidates categories

REVIEW HOOKS (src/features/review/hooks/useReview.ts):
  useReviewWords()            — Query
  useReviewSession(id)        — Query enabled=!!id
  useReviewSummary(id)        — Query enabled=!!id
  useReviewHistory()          — Query
  useStreak()                 — Query
  useDailyProgress()          — Query
  usePredictedIntervals(id)   — Query enabled=!!id
  useEnrichmentStatus()       — Query refetchInterval=3s (auto-polling)
  useStartSession()           — Mutation → reviewStore.setSession
  useSubmitAnswer()           — Mutation
  useCompleteSession()        — Mutation → invalidates review queries
  useEnrichWord()             — Mutation → invalidates words + enrichment
  useEnrichAll()              — Mutation → invalidates enrichment

TEST HOOKS (src/features/tests/hooks/useTests.ts):
  useTestHistory()            — Query
  useTestDetail(id)           — Query enabled=!!id
  useGenerateTest()           — Mutation
  useSubmitTestAnswer()       — Mutation
  useCompleteTest()           — Mutation → invalidates test queries

GAME HOOKS (src/features/games/hooks/useGames.ts):
  useGameHistory()            — Query
  useGameStats()              — Query
  useStartSpeedRound()        — Mutation
  useSubmitSpeedRound()       — Mutation → invalidates game queries
  useStartWordMatch()         — Mutation
  useSubmitWordMatch()        — Mutation
  useStartWordContext()       — Mutation
  useSubmitWordContext()      — Mutation
  useStartStoryBuilder()      — Mutation
  useSubmitStoryBuilder()     — Mutation
  useStartListening()         — Mutation
  useSubmitListening()        — Mutation

CHAT HOOKS (src/features/chat/hooks/useChat.ts):
  useChatHistory()            — Query
  useChatSession(id)          — Query enabled=!!id
  useStartChat()              — Mutation
  useSendMessage()            — Mutation → optimistic cache update
  useEndChat()                — Mutation → invalidates chat queries

IMPORT HOOKS (src/features/import/hooks/useImport.ts):
  useAnalyzeText()            — Mutation
  useImportWords()            — Mutation → invalidates words

ANALYTICS HOOKS (src/features/analytics/hooks/useAnalytics.ts):
  useAnalyticsOverview()      — Query ['analytics','overview']
  useWeeklyStats()            — Query ['analytics','weekly']
  useMonthlyStats()           — Query ['analytics','monthly']
  useDifficultWords()         — Query ['analytics','difficultWords']
  useWordProgress()           — Query ['analytics','wordProgress']
  useStudyCalendar()          — Query ['analytics','calendar']

PROGRESS HOOKS (src/features/progress/hooks/useProgress.ts):
  useUserProgress()           — Query
  useXPHistory()              — Query
  useUserBadges()             — Query
  useAllBadges()              — Query
  useNotifications()          — Query
  useUnreadNotificationCount() — Query refetchInterval=30s (auto-polling)
  useMarkNotificationRead()   — Mutation → invalidates notifications
  useMarkAllNotificationsRead() — Mutation → invalidates notifications

CHALLENGES HOOKS:
  useDailyChallenges()        — Query ['challenges','today']
  useClaimBonus()             — Mutation → invalidates challenges

CONFUSING PAIRS HOOKS:
  useConfusingPairs()         — Query
  useConfusingPairDetail(id)  — Query enabled=!!id
  useUnresolvedCount()        — Query
  useGenerateDrill()          — Mutation
  useResolvePair()            — Mutation → invalidates pairs

ONBOARDING HOOKS:
  useOnboardingQuestions()    — Query
  useSubmitOnboarding()       — Mutation
  useSkipOnboarding()         — Mutation
  useOnboardingStatus()       — Query

GLOBAL HOOKS:
  useDebounce<T>(value, delay=300) — Generic debounce
  useMediaQuery(query)              — CSS media query match
  useTheme()                        — {theme, setTheme} dan ThemeContext


================================================================================
8. TIP TIZIMI (Type System)
================================================================================

Barcha tiplar src/types/ papkada, index.ts orqali re-export qilinadi.
15 ta fayl, 50+ ta tip/interfeys.

CORE TIPLER (core.ts):
  Theme              = 'light' | 'dark' | 'system'
  DifficultyLevel    = 'easy' | 'medium' | 'hard'
  ProficiencyLevel   = 'A1' | 'A2' | 'B1' | 'B2' | 'C1' | 'C2'
  ReviewQuality      = 0 | 1 | 2 | 3 | 4 | 5
  EnrichmentStatus   = 'pending' | 'processing' | 'completed' | 'failed'
  TestType           = 'multiple_choice' | 'fill_blank' | 'context_guess' |
                       'mixed'
  GameType           = 'speed_round' | 'word_match' | 'word_context' |
                       'story_builder' | 'listening_challenge'
  BadgeRarity        = 'common' | 'rare' | 'epic' | 'legendary'
  NotificationType   = 'badge' | 'streak' | 'reminder' | 'achievement' |
                       'system'

API TIPLER (api.ts):
  ApiResponse<T>     — {success, data, message?, errors?, meta?}
  PaginationMeta     — {count, page, page_size, total_pages, next, previous}

WORD TIPLER (word.ts):
  Word               — 30+ maydon (id, original_word, translation, definition,
                       pronunciation, part_of_speech, difficulty_level,
                       synonyms, antonyms, mnemonic, usage_notes,
                       collocations, enrichment_status, easiness_factor,
                       SM-2 maydonlar, review maydonlar, timestamps)
  WordCategory       — {id, name, color?, word_count?}
  WordCreateData, WordUpdateData, BulkWordCreateData
  WordStats          — {total_words, by_difficulty, by_enrichment,
                       mastered_count, learning_count, new_count}
  WordFilters        — {search, difficulty, category, ordering, page, size}

USER TIPLER (user.ts):
  User               — {id, email, username, names, languages, level,
                       daily_goal, premium, streak, xp, avatar, bio,
                       timezone, onboarding, timestamps}
  AuthTokens, LoginCredentials, RegisterData, ChangePasswordData,
  UpdateProfileData

REVIEW TIPLER (review.ts):
  ReviewSession, ReviewAnswer, ReviewAnswerResult, ReviewSummary,
  DailyStreak, DailyProgress, EnrichmentStatusResponse,
  PredictedIntervals

TEST TIPLER (test.ts):
  TestQuestion, TestSession, TestGenerateRequest/Response,
  TestAnswerRequest/Response

GAME TIPLER (game.ts):
  GameSession, SpeedRoundWord, SpeedRoundStartResponse, SpeedRoundAnswer,
  WordMatchPair, WordContextQuestion, StoryBuilderData, ListeningData,
  GameStats

CHAT TIPLER (chat.ts):
  ChatMessage, ChatCorrection, ChatSession,
  StartChatRequest/Response, SendMessageRequest/Response

BADGE TIPLER (badge.ts):
  BadgeData, UserBadgesResponse, NewBadgeInfo

PROGRESS TIPLER (progress.ts):
  UserProgressData, XPHistoryEntry, XPResult

NOTIFICATION TIPLER (notification.ts):
  NotificationData, NotificationListResponse

SMART IMPORT TIPLER (smart-import.ts):
  WordSuggestion, AnalyzeTextRequest/Response,
  ImportWordsRequest/Response

ANALYTICS TIPLER (analytics.ts):
  AnalyticsOverview, DailyStatsEntry, DifficultWord,
  WordProgressData, CalendarDay


================================================================================
9. KOMPONENT ARXITEKTURASI
================================================================================

LAYOUT KOMPONENTLARI (src/components/layout/):

  RootLayout
    - TopBar + Sidebar (desktop: ≥1024px, useMediaQuery)
    - MobileNav (mobil: <1024px)
    - <Outlet /> (sahifa kontenti)

  AuthLayout
    - Markazlashtirilgan layout
    - Logo + <Outlet />

  Sidebar
    - 12 ta navigatsiya elementi (yangi xususiyatlar bilan)
    - Framer Motion bilan kengaytirish/yig'ish animatsiya
    - useAppStore dan sidebarCollapsed

  TopBar
    - Qidiruv input
    - LevelProgress (compact)
    - StreakWidget (compact)
    - DailyChallengesWidget (bugungi topshiriqlar)
    - Tema almashtirgich (light → dark → system)
    - NotificationBell (dropdown + o'qilmagan soni)
    - Chiqish tugmasi
    - Foydalanuvchi avatar

  MobileNav
    - Fixed bottom navigation (5 ta element)
    - Home, Words, Tests, Games, Profile

  Sidebar navigatsiya elementlari:
    Home (/), Words (/words), Import (/import), Review (/review),
    Chat (/chat), Tests (/tests), Games (/games), Analytics (/analytics),
    Confusing Pairs (/confusing-pairs), Challenges (widget),
    Badges (/badges), Profile (/profile)

UMUMIY KOMPONENTLAR (src/components/common/):
  LoadingScreen    — Animatsiyalangan BookOpen icon + 3 ta sakrash nuqtasi
  Logo             — {collapsed?} — BookOpen icon + "WordFix" matn
  EmptyState       — {icon, title, description, action?}
  ErrorBoundary    — Class component, retry tugma bilan xato cheklov

UI KOMPONENTLAR (src/components/ui/) — shadcn/ui uslubida CVA bilan:
  badge, button, card (Card/CardHeader/CardTitle/CardDescription/
  CardContent/CardFooter), checkbox, input, progress, separator,
  skeleton, switch, textarea

ANIMATSIYA KOMPONENTLARI (src/components/animations/):
  PageTransition        — motion.div: fade + slide-up
  listContainerVariants — Bolalar 0.05s interval bilan
  listItemVariants      — Fade + slide-up animatsiya
  cardHoverVariants     — Scale 1.02 + soya hover da


================================================================================
10. FEATURE SAHIFALARI — BATAFSIL
================================================================================

=== AUTH FEATURE (src/features/auth/) ===

  LoginPage
    - Email + parol formasi (validatsiya bilan)
    - "Meni esla" checkbox
    - Register ga havola
    - Google OAuth tugma
    - useAuthStore().login() → / ga navigatsiya

  RegisterPage
    - 3 bosqichli wizard:
      1-bosqich: email, username, parol, parol tasdiqlash
      2-bosqich: ism/familiya, ona tili, o'rganish tili, daraja
      3-bosqich: ko'rib chiqish va tasdiqlash
    - Parol kuchlilik ko'rsatgichi
    - useAuthStore().register() → / yoki /onboarding ga navigatsiya

=== ONBOARDING FEATURE (src/features/onboarding/) ===

  OnboardingPage
    - Daraja aniqlash testi (savol-javob)
    - OnboardingProgress — progress bar
    - OnboardingQuestion — har bir savol kartasi
    - LevelIndicator — natija ko'rsatish (A1–C2)
    - OnboardingResult — yakuniy natija + tavsiyalar
    - SkipButton — o'tkazib yuborish imkoniyati
    - Natija asosida proficiency_level avtomatik o'rnatiladi

=== DASHBOARD FEATURE (src/features/dashboard/) ===

  DashboardPage
    - Xush kelibsiz xabari (user.first_name)
    - LevelProgress (to'liq, XP bar bilan)
    - StreakWidget (to'liq, olov animatsiya)
    - DailyChallengesWidget (bugungi topshiriqlar)
    - DailyProgressWidget (maqsadga qarab progress)
    - XPChart (7 kunlik bar chart)
    - Review CTA (kutayotgan so'zlar soni)
    - Test va Games tezkor harakatlar
    - 4 ta statistika karta (Total Words, Mastered, Avg Accuracy,
      Current Streak)
    - Oxirgi 5 ta so'z ro'yxati

=== LANDING FEATURE (src/features/landing/) ===

  LandingPage
    - Hero section: gradient matn + CTA tugmalar (Register/Login)
    - 8 ta xususiyat kartasi: Spaced Repetition, AI Enrichment,
      Smart Import, Practice Tests, Word Games, Chat Practice,
      Daily Challenges, Confusing Pairs
    - Statistika paneli (animatsiyalangan raqamlar)
    - Footer CTA
    - Faqat autentifikatsiya qilinmagan foydalanuvchilar uchun

=== WORDS FEATURE (src/features/words/) ===

  WordsPage
    - Grid/list ko'rinish almashtirgich
    - Qidiruv input (debounced, 300ms)
    - Qiyinlik filtr dropdown (easy/medium/hard/all)
    - Kategoriya filtr
    - So'z kartalari (aylantirish = tarjima ko'rish, flip animatsiya)
    - Enrichment status badge (pending/enriching/enriched/failed)
    - Pagination
    - "So'z qo'shish" → AddWordModal
    - O'chirish tasdiqlash dialog bilan
    - Bulk qo'shish imkoniyati

  AddWordModal
    - Dialog forma: so'z, tarjima, ta'rif, misol, qiyinlik (select),
      kategoriya (select + yangi yaratish), eslatmalar (textarea)
    - useCreateWord() mutation
    - Auto-enrich (yangi so'z qo'shilganda AI boyitish boshlanadi)

=== REVIEW FEATURE (src/features/review/) ===

  ReviewPage
    - Sessiya turi tanlash kartalari (review/learn/mixed)
    - StreakWidget (to'liq ko'rinish)
    - DailyProgressWidget
    - Ko'rib chiqish xulosa statistikasi
    - Kutayotgan so'zlar oldindan ko'rish (scroll)

  ReviewSessionPage
    - FlashCard (3D CSS flip animatsiya):
      Old tomon: so'z + talaffuz + audio play tugma
      Orqa tomon: tarjima + ta'rif + misol + mnemonic + sinonimlar +
                  antonimlar + usage_notes
    - QualityRating (6 ta tugma, emoji bilan):
      0=Again 😫, 1=Hard 😰, 2=Difficult 😐,
      3=Good 🙂, 4=Easy 😊, 5=Perfect 🤩
    - Progress bar (animated)
    - Navigatsiya o'qlari (← →)
    - Yuborish/tugatish tugmalari
    - useReviewStore (local state) + useSubmitAnswer/useCompleteSession

  ReviewCompletePage
    - Confetti animatsiya (canvas-confetti, 3 soniya)
    - Sessiya statistikasi (aniqlik %, XP earned, vaqt)
    - Yangi badgelar ko'rsatish (modal)
    - "Xulosani ko'rish" / "Orqaga" harakatlar

  ReviewHistoryPage
    - Paginated sessiya ro'yxati
    - Sessiya turi badge, so'z soni, aniqlik %, XP, sana

=== TESTS FEATURE (src/features/tests/) ===

  TestPage
    - Tur tanlash kartalari: multiple_choice / fill_blank /
      context_guess / mixed
    - Qiyinlik tanlash (easy/medium/hard/adaptive)
    - Savol soni slider (5-30, 5 qadam)
    - Oxirgi test tarixi
    - useGenerateTest() → sessiya sahifasiga navigatsiya

  TestSessionPage
    - Savol-savol UI:
      multiple_choice = variant tugmalari (click = auto-submit)
      fill_blank = matn input + yuborish tugma
      context_guess = paragraf ko'rsatish + variantlar
    - Yashil/qizil feedback animatsiya
    - Progress ko'rsatgich (savol raqami / jami)
    - Timer (optional)
    - useSubmitTestAnswer(), useCompleteTest()

  TestResultPage
    - Harf bahosi (A-F) katta ko'rinishda
    - Foiz ball (doira animatsiya)
    - XP topilgan
    - Savol ko'rib chiqish ro'yxati (to'g'ri ✓ / noto'g'ri ✗)
    - "Yana test" / "Orqaga" tugmalari

=== GAMES FEATURE (src/features/games/) ===

  GamesPage
    - 5 ta o'yin kartasi (animatsiyalangan):
      1. Speed Round — Tezlik o'yini (60 soniya)
      2. Word Match — So'z juftlash
      3. Word Context — Kontekstdan topish
      4. Story Builder — AI hikoya davom ettirish
      5. Listening Challenge — Eshitib yozish
    - Har bir o'yin turi statistikasi
    - Oxirgi o'yin tarixi

  SpeedRoundPage
    - 60 soniya ortga hisoblash taymeri (doira animatsiya)
    - So'z ko'rsatiladi, 4 ta tarjima varianti
    - Yashil/qizil flash feedback (tez)
    - Avtomatik keyingisiga o'tish
    - Ball hisoblagich + combo streak
    - useStartSpeedRound() + useSubmitSpeedRound()

  WordMatchPage
    - Ikki ustun (so'zlar chapda / tarjimalar o'ngda)
    - Bosish bilan juft tanlash (highlight animatsiya)
    - Mos juftlar yo'qoladi (fade-out)
    - Timer + ball
    - Tugatish aniqlash (barcha juftlar topilganda)

  WordContextPage
    - Kontekst paragraf gap bilan (______)
    - 4 ta so'z varianti
    - Har bir savol uchun feedback + tushuntirish
    - Ketma-ket davom etish

  StoryBuilderPage
    - AI paragraf generatsiya (hikoya boshlanishi)
    - Foydalanuvchi davom ettiradi (textarea)
    - AI baholaydi (grammatika tuzatishlari + ball)
    - Yangi paragraf bilan davom etish
    - ChatMessage uslubidagi UI

  ListeningChallengePage
    - Audio play tugma (so'z eshitish)
    - Matn input (eshitilgan so'zni yozish)
    - Hint tugma (birinchi harf ko'rsatish)
    - To'g'ri/noto'g'ri feedback

  GameResultPage
    - 1-3 yulduz (ball foiziga qarab, animatsiyalangan)
    - Umumiy ball, XP, aniqlik
    - Combo streak statistika
    - "Yana o'ynash" / "Orqaga" tugmalari

=== CHAT FEATURE (src/features/chat/) ===

  ChatPage
    - 6 ta mavzu taklifi kartasi (ikonka bilan):
      Sayohat, Oziq-ovqat, Texnologiya, Sport, Filmlar, Kundalik hayot
    - Maxsus mavzu input (foydalanuvchi o'zi yozishi mumkin)
    - Oxirgi sessiyalar ro'yxati
    - useStartChat() → sessiya sahifasiga navigatsiya

  ChatSessionPage
    - Xabar pufakchalari (user=o'ng/ko'k, assistant=chap/kulrang)
    - Tuzatishlar inline ko'rsatish:
      {original} → {corrected} (qizil → yashil) + izoh
    - Maqsadli so'zlar paneli (tepada, badge ko'rinishda)
    - Avtomatik pastga scroll (yangi xabar kelganda)
    - Xabar input + yuborish tugma (Enter/tugma)
    - Typing indikator (AI javob kutayotganda)
    - "Chat tugatish" tugma
    - useSendMessage() (optimistic yangilash) + useEndChat()

  ChatHistoryPage
    - Paginated sessiya ro'yxati
    - Mavzu, xabar soni, XP, sana
    - Faol/tugatilgan status badge

=== IMPORT FEATURE (src/features/import/) ===

  ImportPage
    - 3 bosqichli wizard (stepper bilan):
      1-bosqich: textarea (matn kiritish, max 5000 belgi) +
                 ixtiyoriy til sozlamalari (source/target)
      2-bosqich: AI matnni tahlil qiladi (useAnalyzeText()),
                 Loading animatsiya,
                 So'z takliflari ro'yxati (ishonch darajasi badge bilan),
                 Checkbox bilan tanlash + Select All / Deselect All
      3-bosqich: Tanlangan so'zlarni import (useImportWords()),
                 Muvaffaqiyat / O'tkazilgan / Xato soni ko'rsatish
    - Orqaga / Oldinga navigatsiya

=== CONFUSING PAIRS FEATURE (src/features/confusing-pairs/) ===

  ConfusingPairsPage
    - Chalkash so'z juftliklari ro'yxati
    - ConfusingPairCard — ikkita so'z va chalkashish soni
    - ConfusingPairBadge — hal qilingan/qilinmagan status
    - DrillModal — AI mashq (savollar + tushuntirish)
    - Resolve tugma (juftlikni o'zlashtirilgan deb belgilash)
    - Hal qilinmaganlar soni badge

=== CHALLENGES FEATURE (src/features/challenges/) ===

  DailyChallengesWidget (Dashboard va TopBar da ko'rsatiladi):
    - ChallengeCard — har bir topshiriq (tavsif + progress + status)
    - ChallengeBonusModal — barcha topshiriqlar bajarilganda bonus olish
    - Kunlik yangilanish

=== ANALYTICS FEATURE (src/features/analytics/) ===

  AnalyticsPage
    - 8 ta StatCard (Total Words, Mastered, Reviews, Tests, Games,
      Accuracy, XP, Streak) — animatsiyalangan raqamlar
    - WeeklyChart (7 kunlik bar chart, framer-motion)
    - StudyCalendar (GitHub uslubidagi heatmap grid, 4 daraja)
    - WordDistribution (confidence + difficulty bo'yicha gorizontal barlar)
    - DifficultWordsList (top 10, aniqlik progress barlari bilan)

=== PROGRESS FEATURE (src/features/progress/) ===

  BadgesPage
    - Kategoriya filtr tablari (all/learning/streak/review/social/
      achievement)
    - BadgeGrid + BadgeCard komponentlari
    - Nodir rang kodlash:
      common=kulrang, rare=ko'k, epic=binafsha, legendary=sariq
    - Olingan (rang) / Qulflangan (kulrang) holat
    - Qisman progress bar

  NotificationsPage
    - "Hammasini o'qilgan" tugma (tepada)
    - Paginated bildirishnoma ro'yxati
    - Har bir bildirishnoma turi icon + o'qilgan/o'qilmagan uslub
    - Har biriga "O'qilgan" tugma

=== PROFILE FEATURE (src/features/profile/) ===

  ProfilePage
    - Ko'rsatish rejimi: avatar, ism, email, tillar, daraja, kunlik
      maqsad, bio, statistika
    - Tahrirlash rejimi: ism, bio, tillar, daraja, kunlik maqsad,
      timezone tanlash (select)
    - Parol almashtirish bo'limi (eski + yangi + tasdiqlash)
    - Avatar yuklash imkoniyati


================================================================================
11. FEATURE ICHKI KOMPONENTLARI
================================================================================

REVIEW KOMPONENTLARI:
  FlashCard           — {word, isFlipped, onFlip} — 3D CSS flip karta
  QualityRating       — {onRate, disabled} — 6 ta emoji tugma
  StreakWidget         — {compact?} — streak + olov icon, useStreak()
  DailyProgressWidget — maqsad progress, so'zlar, XP, sessiyalar, aniqlik

PROGRESS KOMPONENTLARI:
  LevelProgress       — {compact?} — daraja raqami + XP progress bar
  XPChart             — 7 kunlik bar chart (framer-motion animatsiya)
  XPPopup             — XP topilganda suzuvchi bildirishnoma
  BadgeCard           — {badge} — icon, nom, tavsif, nodir rang, holat
  BadgeGrid           — {badges, filter?} — responsive grid
  NotificationBell    — dropdown + o'qilmagan soni badge

ANALYTICS KOMPONENTLARI:
  StatCard            — {icon, label, value, subtitle?, color?}
  WeeklyChart         — {data} — animatsiyalangan bar chart
  StudyCalendar       — {data} — GitHub uslubidagi heatmap grid
  DifficultWordsList  — {words} — top 10 + aniqlik progress barlari
  WordDistribution    — {byConfidence, byDifficulty} — gorizontal barlar

CHALLENGES KOMPONENTLARI:
  ChallengeCard           — {challenge} — topshiriq kartasi
  ChallengeBonusModal     — bonus olish modali
  DailyChallengesWidget   — {compact?} — kunlik topshiriqlar widget

CONFUSING PAIRS KOMPONENTLARI:
  ConfusingPairCard       — {pair} — ikkita so'z ko'rsatish
  ConfusingPairBadge      — {resolved} — status badge
  DrillModal              — {pair} — AI mashq modal

ONBOARDING KOMPONENTLARI:
  OnboardingProgress      — progress bar (bosqichlar)
  OnboardingQuestion      — savol kartasi + variantlar
  LevelIndicator          — natija ko'rsatish (A1–C2)
  OnboardingResult        — yakuniy natija + tavsiyalar
  SkipButton              — o'tkazib yuborish


================================================================================
12. STYLING VA TEMA
================================================================================

CSS O'ZGARUVCHILARI (src/index.css):
  HSL asosidagi custom properties:
    :root (light) va .dark (dark) klasslari:
    --background, --foreground, --card, --popover, --primary,
    --secondary, --muted, --accent, --destructive, --border,
    --input, --ring
    Har birida -foreground qarshi tomoni
    --radius: 0.5rem

MAXSUS UTILITALAR:
  .text-gradient — bg-gradient-to-r from-primary via-purple-500 to-pink-500
                   bg-clip-text text-transparent
  .glass         — backdrop-blur-xl bg-white/80 dark:bg-gray-900/80
                   border bilan (glassmorphism effekt)
  Custom scrollbar — yupqa, muted ranglar

TEMA TIZIMI (ThemeProvider):
  3 ta rejim: light, dark, system
  localStorage da saqlanadi (kalit: "wordfix-theme")
  system rejimi: prefers-color-scheme: dark media query tinglaydi
  <html> ga dark yoki light klass qo'shadi

RESPONSIVE DIZAYN:
  - Mobil (< 768px): MobileNav, bitta ustunli layout
  - Planshet (768-1023px): compact Sidebar
  - Desktop (≥ 1024px): to'liq Sidebar + TopBar


================================================================================
13. LIB UTILITALARI
================================================================================

src/lib/utils.ts:
  cn(...inputs) — clsx + twMerge (Tailwind class birlashtirish,
                  konfliktsiz)

src/lib/constants.ts:
  APP_NAME          = 'WordFix'
  APP_VERSION       = '1.0.0'
  THEME_STORAGE_KEY = 'wordfix-theme'
  BREAKPOINTS       = {sm:640, md:768, lg:1024, xl:1280, 2xl:1536}
  NAV_ITEMS         = [
    {label:'Home',       path:'/',                icon:Home},
    {label:'Words',      path:'/words',           icon:BookOpen},
    {label:'Import',     path:'/import',          icon:Upload},
    {label:'Review',     path:'/review',          icon:RotateCcw},
    {label:'Chat',       path:'/chat',            icon:MessageSquare},
    {label:'Tests',      path:'/tests',           icon:ClipboardCheck},
    {label:'Games',      path:'/games',           icon:Gamepad2},
    {label:'Analytics',  path:'/analytics',       icon:BarChart3},
    {label:'Confusing',  path:'/confusing-pairs', icon:Shuffle},
    {label:'Badges',     path:'/badges',          icon:Award},
    {label:'Profile',    path:'/profile',         icon:User},
  ]


================================================================================
14. FAYL STRUKTURASI — FRONTEND
================================================================================

wordfix-frontend/
├── index.html
├── package.json
├── vite.config.ts
├── tailwind.config.ts
├── tsconfig.json
├── tsconfig.node.json
├── postcss.config.js
├── components.json            — shadcn/ui konfiguratsiya
├── Dockerfile
├── public/assets/images/
├── src/
│   ├── main.tsx               — Entry point
│   ├── App.tsx                — Root component
│   ├── index.css              — Global CSS + tema
│   ├── vite-env.d.ts
│   ├── api/
│   │   ├── client.ts          — Axios instance + interceptors
│   │   └── endpoints.ts       — API endpoint konstantalar
│   ├── assets/
│   │   ├── fonts/             — Inter, Space Grotesk
│   │   └── illustrations/
│   ├── components/
│   │   ├── animations/PageTransition.tsx
│   │   ├── common/LoadingScreen.tsx, Logo.tsx, EmptyState.tsx,
│   │   │          ErrorBoundary.tsx
│   │   ├── layout/RootLayout.tsx, AuthLayout.tsx, Sidebar.tsx,
│   │   │          TopBar.tsx, MobileNav.tsx
│   │   └── ui/badge.tsx, button.tsx, card.tsx, checkbox.tsx,
│   │          input.tsx, progress.tsx, separator.tsx,
│   │          skeleton.tsx, switch.tsx, textarea.tsx
│   ├── features/
│   │   ├── analytics/         — api/, hooks/, components/, pages/
│   │   ├── auth/              — api/, pages/
│   │   ├── challenges/        — api/, hooks/, components/, types/
│   │   ├── chat/              — api/, hooks/, pages/
│   │   ├── confusing-pairs/   — api/, hooks/, components/, pages/, types/
│   │   ├── dashboard/         — pages/
│   │   ├── games/             — api/, hooks/, pages/
│   │   ├── import/            — api/, hooks/, pages/
│   │   ├── landing/           — pages/
│   │   ├── onboarding/        — api/, hooks/, components/, pages/, types/
│   │   ├── profile/           — pages/
│   │   ├── progress/          — api/, hooks/, components/, pages/
│   │   ├── review/            — api/, hooks/, components/, pages/
│   │   ├── tests/             — api/, hooks/, pages/
│   │   └── words/             — api/, hooks/, components/, pages/
│   ├── hooks/
│   │   ├── useDebounce.ts
│   │   ├── useMediaQuery.ts
│   │   └── useTheme.ts
│   ├── lib/
│   │   ├── constants.ts
│   │   └── utils.ts
│   ├── providers/
│   │   ├── QueryProvider.tsx
│   │   └── ThemeProvider.tsx
│   ├── routes/index.tsx
│   ├── stores/
│   │   ├── useAppStore.ts
│   │   ├── useAuthStore.ts
│   │   └── useReviewStore.ts
│   ├── styles/globals.css
│   └── types/
│       ├── index.ts           — Barcha tiplarni re-export
│       ├── core.ts, api.ts, word.ts, user.ts, review.ts
│       ├── test.ts, game.ts, chat.ts, badge.ts
│       ├── progress.ts, notification.ts, smart-import.ts
│       └── analytics.ts

  JAMI 15 ta feature papka, 100+ ta komponent fayli

================================================================================
              FRONTEND HUJJAT TUGADI — 14 BO'LIM
================================================================================
